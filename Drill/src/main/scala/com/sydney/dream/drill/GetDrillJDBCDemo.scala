package com.sydney.dream.drill

import java.sql.DriverManager
// 获取Drill jdbc 连接，并获取数据的条数
object GetDrillJDBCDemo {
    def main(args: Array[String]) {
//        Class.forName("org.apache.drill.jdbc.Driver")
//        val conn = DriverManager.getConnection("jdbc:drill:zk=s100:2181,s101:2181,s102:2181/drill/mydirllcluster")
//        val statement = conn.createStatement
//        val results = statement.executeQuery("select count(*) from dfs.`/user/hive/warehouse/person_table`")
//        while (results.next) {
//            println(results.getInt(1))
//        }
//        conn.close

        String.valueOf()
    }
}
